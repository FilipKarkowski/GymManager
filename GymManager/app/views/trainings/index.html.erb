<div class="p-4">
  <p class="text-green-500"><%= notice %></p>

  <h1 class="text-3xl font-bold mb-4">Treningi</h1>

  <div id="trainings">
    <% @trainings.each do |training| %>
      <div class="border rounded-md p-4 mb-4">
        <h2 class="text-xl font-semibold mb-2"><%= training.name %></h2>
        <p class="text-gray-600"><%= simple_format(training.description) %></p>
        <p class="mt-2">
          <%= link_to "Pokaż ten trening", training, class: "text-blue-500 hover:underline" %>
        </p>
      </div>
    <% end %>
  </div>

  <%= link_to "Nowy trening", new_training_path, class: "bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 mt-4 inline-block" %>
</div>
